meta {
  name: health
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/health
  body: none
  auth: none
}

headers {
  x-trace-key: {{xAdminKey}}
}

tests {
  test("http response is fine", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });

  test("response status is OK", function() {
    const data = res.getBody();
    expect(data.status).to.equal('OK');
  });
}
