meta {
  name: create user
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: none
}

body:json {
  {
    "name": "Wetzel"
  }
}

vars:post-response {
  userId: res.body.userId
}

assert {
  res.status: eq 201
}
